generator js {
  provider = "prisma-client-js"
}
datasource db {
  provider = "postgresql"
  // url      = env("DATABASE_URL")
  url      = "postgresql://weather:fugewjrlwerjweldoifjv4j829v82398h2f9h298d9j2d92j3d889dj28djdovi1221@46.175.147.63:9011/weather?schema=public"
}

model User {
  id Int @id @default(autoincrement())

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  email      String  @unique
  username   String? @unique
  firstname  String?
  lastname   String?
  avatarUrl  String?
  webpageUrl String?

  hash     String
  hashedRt String?

  weatherStations WeatherStation[]
}

model WeatherStation {
  id                  String          @id @default(uuid())
  latitude            Float
  longitude           Float
  api_key             String          @unique
  User                User?           @relation(fields: [userId], references: [id])
  userId              Int?
  weatherRecordSchema Json
  weatherRecords      WeatherRecord[]
}

model WeatherRecord {
  id               Int             @id @default(autoincrement())
  createdAt        DateTime        @default(now())
  weatherRecord    Json
  WeatherStation   WeatherStation? @relation(fields: [weatherStationId], references: [id])
  weatherStationId String?
}
