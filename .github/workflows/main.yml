name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  nestjs:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install dependencies
        run: yarn install

      - name: Build
        run: yarn workspace weather-server build

      - name: Test
        run: yarn workspace weather-server test

      - name: Start production server
        run: yarn workspace weather-server start:prod

  vite:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install dependencies
        run: yarn install

      - name: Build
        run: yarn workspace vite build

      - name: Test
        run: yarn workspace vite test

      - name: Start server
        run: yarn workspace vite serve

  python:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.9"

      - name: Install dependencies
        run: |
          cd weather-algo
          python -m venv venv
          source venv/bin/activate
          pip install -r requirements.txt

      - name: Start server
        run: |
          cd weather-algo
          source venv/bin/activate
          flask run --port=9002
