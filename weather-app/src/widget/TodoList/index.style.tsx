import tw, { styled } from 'twin.macro'
import { Button, WidgetCard } from '../../shared/ui'

export const WidgetCard = tw(WidgetCard)`
  max-w-7xl m-auto mt-3
`

export const StyledButton = tw(Button)`
  h-20 bg-sky-600 ml-2 mb-3
`
